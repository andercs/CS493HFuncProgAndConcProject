# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.Application.index()
GET		/features					controllers.Application.features()
GET		/screenshots				controllers.Application.screenshots()
GET		/announcements				controllers.Application.announcements()
GET		/about						controllers.Application.aboutUs()
GET		/summary					controllers.Application.projectSummary()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /webjars/*file              controllers.WebJarAssets.at(file)
